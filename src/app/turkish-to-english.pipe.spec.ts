import { TurkishToEnglishPipe } from './turkish-to-english.pipe';

describe('TurkishToEnglishPipe', () => {
  it('create an instance', () => {
    const pipe = new TurkishToEnglishPipe();
    expect(pipe).toBeTruthy();
  });
});
