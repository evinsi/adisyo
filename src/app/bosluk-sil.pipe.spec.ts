import { BoslukSilPipe } from './bosluk-sil.pipe';

describe('BoslukSilPipe', () => {
  it('create an instance', () => {
    const pipe = new BoslukSilPipe();
    expect(pipe).toBeTruthy();
  });
});
